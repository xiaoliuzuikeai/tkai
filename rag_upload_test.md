# 星桥知识库测试文档

## 文档用途

本文档仅用于测试 GoAI 的 RAG 文件上传、文本切分、向量索引和知识检索功能。文档中的组织、项目和数据均为虚构内容，不代表真实业务信息。

## 项目概况

星桥计划的项目编号是 `XQ-2026-0805`，项目负责人是林澈。项目于 2026 年 8 月 5 日启动，目标是建设一个面向内部员工的智能知识问答系统。项目使用“北辰”作为内部代号，计划在 2026 年 12 月 20 日完成第一阶段验收。

星桥计划的服务时间为工作日 09:00 至 18:00。紧急问题由蓝鲸小组处理，普通问题由云杉小组处理。项目支持邮箱提交和系统工单两种反馈渠道，但测试环境不发送真实邮件。

## 系统组成

系统由四个主要部分组成：

1. 文档上传服务：接收 Markdown 或纯文本文件。
2. 文本处理服务：将文档切分为适合检索的文本块。
3. 向量检索服务：保存文本向量并查询相关内容。
4. 对话服务：结合检索结果生成回答，并返回引用来源。

上传文件的允许格式为 `.md` 和 `.txt`，单个文件最大为 5 MiB。上传成功后，文档状态应变为 `ready`；如果索引失败，文档状态会变为 `failed`，此时可以调用重试接口重新建立索引。

## 测试规则

测试口令是“银杏-4729”。该口令仅用于确认检索结果是否来自本文档，不用于任何身份验证。质量检查编号是 `QA-RAG-1357`。

当用户询问“星桥计划的负责人是谁”时，预期答案为“林澈”。当用户询问“第一阶段验收日期”时，预期答案为“2026 年 12 月 20 日”。当用户询问“紧急问题由哪个小组处理”时，预期答案为“蓝鲸小组”。

## 常见问题

### 如何判断上传成功？

接口响应中的 `status_code` 应为 `1000`，文档对象的 `status` 应为 `ready`，并且 `chunk_count` 应大于零。

### 上传成功后如何测试检索？

创建一个使用模型类型 `2` 的 RAG 会话，然后提问本文档中的具体内容。例如：

- 星桥计划的项目编号是什么？
- 星桥计划的测试口令是什么？
- 哪个小组负责紧急问题？
- 系统允许上传哪些文件格式？

### 如果上传失败怎么办？

首先确认文件扩展名、文件大小、登录 Token、向量模型配置和 Redis 服务。若文档已经生成但状态为 `failed`，可以使用文档重试接口重新索引。

## 预期检索答案

- 项目编号：XQ-2026-0805
- 项目负责人：林澈
- 内部代号：北辰
- 第一阶段验收日期：2026 年 12 月 20 日
- 紧急问题处理小组：蓝鲸小组
- 普通问题处理小组：云杉小组
- 测试口令：银杏-4729
- 质量检查编号：QA-RAG-1357

